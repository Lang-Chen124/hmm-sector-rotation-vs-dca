# CSI 300 DCA results

Upload the files generated by `沪深300定投基准收益.ipynb` to this directory.

The notebook writes its CSV exports to `baseline_outputs/`. Keep the generated filenames unchanged so that the repository structure remains aligned with the notebook.
